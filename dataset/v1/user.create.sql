-- To use ${form.anything}
-- To use ${param.anything}
-- To use ${query.anything}

-- anrinch.com/api/user/:`param`?`query`=one
-- body sent in `form`

SELECT * FROM ${form.database} WHERE test = ${params.test}
